services:
  - type: web
    name: farmdirect-ai-backend
    runtime: python
    rootDir: AI_backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /
